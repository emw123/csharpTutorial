// Stack example 1
static void Main() {
    // Create a new Stack
    Stack newStack = new Stack();
    // Add items to stack
    newStack.push(1);
    newStack.push(2);
    newStack.push(3);
    // Print the stack
    Console.Write(newStack);
}